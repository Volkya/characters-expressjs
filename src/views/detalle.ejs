<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Character profile</title>
    <%- include("partials/head") -%>
</head>
<body>
    <%- include("partials/navegation") %>

    <div class="container p-4">
        <div class="row">
            <div class="col-md-4 offset-md-4">
                <div class="card animate fadeIndown">
                    <img src="<%= image.path %>" alt="<%= image.filename %>" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= image.title %>
                        </h5>
                        <p class="card-text">
                            <%= image.description %>
                        </p>
                        <p>
                            <%= format(image.created_at) %>
                        </p>
                        <a href="/character/<%= image._id %>/delete" class="btn btn-danger btn-block">
                            Delete Character
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>